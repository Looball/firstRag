# 开发设计

## assistant.py 审查对象

审查文件：`assistant.py`

当前职责：

- 初始化本地 Chroma 向量库检索器。
- 使用 `ChatDeepSeek`、提示词模板和 LCEL 组装 RAG 问答链。
- 通过 Streamlit 展示聊天界面。
- 使用 `st.session_state` 保存会话消息和链对象。
- 将模型回答以手动流式渲染方式输出到页面。

## 当前代码整体评价

`assistant.py` 已经具备一个最小可用 RAG 聊天 Demo 的完整闭环：用户输入、问题改写、向量检索、上下文拼接、模型回答、页面展示都已经连起来了。

下一阶段最重要的目标不是继续堆功能，而是先把稳定性、可观测性和代码边界补起来。这样后面增加文档上传、检索片段展示、参数调节和评估功能时，代码不会很快变得难维护。

## 需要优先处理的问题

### 1. 当前用户问题被重复传入模型

位置：`assistant.py:192-203`

当前逻辑是在生成回答前先执行：

```python
st.session_state.messages.append(("human", prompt))
```

随后又把 `input=prompt` 和包含当前 `prompt` 的 `chat_history` 一起传给链。这样提示词里会出现两份当前用户问题：

- 一份来自 `chat_history`。
- 一份来自 `("human", "{input}")`。

影响：

- 第一次提问时，`chat_history` 已经非空，会进入问题改写链，额外调用一次 LLM。
- 当前问题被重复放进 prompt，增加 token 成本。
- 多轮对话时，问题改写和最终回答可能受到重复消息干扰。

建议改法：

```python
history_for_chain = st.session_state.messages.copy()

st.session_state.messages.append(("human", prompt))

answer = get_answer(
    chain=st.session_state.qa_history_chain,
    input=prompt,
    chat_history=history_for_chain,
)
```

也就是：页面可以立即显示当前用户输入，但传给链的历史记录应当是不包含当前问题的旧历史。

### 2. 缺少环境和向量库状态检查

位置：`assistant.py:24-32`、`assistant.py:52-60`

当前代码直接读取：

- `DEEPSEEK_API_KEY`
- `ZAI_EMD_API`
- `./vector_db/chroma`

如果 API Key 不存在、向量库目录不存在、向量库为空，页面可能在加载阶段直接报错。

建议新增启动检查：

- 检查 `DEEPSEEK_API_KEY` 是否存在。
- 检查 `ZAI_EMD_API` 是否存在。
- 检查 `vector_db/chroma` 是否存在。
- 检查 Chroma collection 是否有数据。
- 在 Streamlit 页面用 `st.error` 或 `st.warning` 给出清晰提示。

### 3. 缺少用户可见的错误处理

位置：`assistant.py:199-208`

当前模型调用、检索、流式输出都没有 `try/except`。一旦 API 请求失败、网络失败、Key 失效、向量库损坏，用户会看到完整 traceback。

建议：

- 给 `render_stream(answer)` 外层增加异常捕获。
- 页面提示“生成失败，请检查 API Key、网络或向量库状态”。
- 终端或日志里保留详细错误，页面上只展示友好的摘要。

### 4. 检索结果不可见

位置：`assistant.py:91-132`

当前用户只能看到最终回答，看不到模型参考了哪些文档片段。这不利于学习 RAG，也不利于判断回答是否可靠。

建议下一步增加：

- 展示检索到的原文片段。
- 展示来源文件名、页码、chunk 序号等 metadata。
- 用 `st.expander("查看检索依据")` 折叠展示。
- 如果没有检索到内容，明确提示“知识库中没有找到相关片段”。

### 5. 配置硬编码较多

位置：`assistant.py:24-32`、`assistant.py:53-58`、`assistant.py:102-108`

当前这些值直接写死在代码中：

- 向量库路径：`./vector_db/chroma`
- 检索数量：`k=2`
- 模型名：`deepseek-v4-flash`
- temperature：`0.2`
- max_tokens：`8000`
- 系统 prompt

建议把它们集中到配置区，或拆成 `config.py`：

```python
PERSIST_DIRECTORY = "./vector_db/chroma"
RETRIEVAL_K = 2
MODEL_NAME = "deepseek-v4-flash"
TEMPERATURE = 0.2
MAX_TOKENS = 8000
```

后续也可以在 Streamlit sidebar 中开放部分参数。

## 代码结构提升方向

### 第一阶段：保持单文件，但先变清晰

适合下一次小改。

- 把所有 import 尽量放到文件顶部，保留少数必要的延迟 import。
- 增加配置常量区。
- 增加 `validate_environment()`。
- 增加 `load_vectorstore()` 或 `get_vectorstore()`。
- 修复 chat history 重复传入问题。
- 增加 `try/except` 包住模型生成。
- 增加“清空对话”按钮。

建议目标：`assistant.py` 仍然是单文件，但主流程更稳定。

### 第二阶段：拆分模块

适合功能开始变多时做。

建议拆分为：

```text
assistant.py       # Streamlit UI 入口
rag_chain.py       # get_retriever / get_chain / get_answer
config.py          # 路径、模型、检索参数
prompts.py         # 问题改写 prompt、回答 prompt
utils.py           # 环境检查、格式化文档、错误处理
```

拆分后收益：

- UI 和 RAG 逻辑分离。
- 以后可以复用同一套 RAG 链做 CLI、API 或测试。
- prompt 修改不会干扰页面逻辑。

### 第三阶段：引入测试

适合项目进入稳定迭代后做。

优先测试：

- `get_res_doc()` 能正确拼接文档内容。
- 环境变量缺失时返回清晰错误。
- 空检索结果时不会崩溃。
- chat history 不包含当前输入的重复消息。
- prompt 组装后包含预期字段。

## 四周开发计划

### 第一周：稳定现有聊天链路

目标：让当前 Streamlit RAG 页面稳定可用，减少隐藏 bug、环境问题和直接 traceback。

- [ ] 修复 `chat_history` 重复传入当前用户问题的问题。
- [ ] 增加 `validate_environment()`，检查 `DEEPSEEK_API_KEY` 和 `ZAI_EMD_API` 是否存在。
- [ ] 检查 `vector_db/chroma` 是否存在，并在页面提示知识库状态。
- [ ] 检查 Chroma collection 是否有数据，避免空知识库时直接进入问答。
- [ ] 给模型调用、检索、流式输出增加 `try/except`。
- [ ] 页面错误提示改成用户可读文案，不直接暴露完整 traceback。
- [ ] 增加“清空对话”按钮。
- [ ] 把模型名、向量库路径、`k` 值、temperature、max_tokens 提取成配置常量。
- [ ] 在 sidebar 展示当前模型、向量库路径、检索数量、知识库状态。
- [ ] 保持 `assistant.py` 单文件，不急着大拆分，先让主流程可靠。

第一周验收标准：

- [ ] 没有 API Key 时页面能给出明确提示。
- [ ] 没有向量库或向量库为空时页面能给出明确提示。
- [ ] 第一次提问不会额外触发问题改写链。
- [ ] 当前用户问题不会在 prompt 中重复出现。
- [ ] API 调用失败时页面不崩溃。

### 第二周：让 RAG 过程可解释

目标：用户不仅看到最终答案，还能看到系统为什么这么回答。

- [ ] 在回答下方增加 `st.expander("查看检索依据")`。
- [ ] 展示每次检索到的文档片段内容。
- [ ] 展示文档 metadata，例如来源文件、页码、chunk 序号。
- [ ] 展示本轮问题改写后的独立问题，方便观察多轮对话效果。
- [ ] 支持在 sidebar 调整检索数量 `k`。
- [ ] 增加“仅根据知识库回答”和“允许模型补充说明”两种回答模式。
- [ ] 当没有检索到相关内容时，明确提示“知识库中没有找到相关片段”。
- [ ] 把每次问答的输入问题、改写问题、检索片段、回答结果记录到本地日志文件。
- [ ] 初步整理一批固定测试问题，作为后续 RAG 评估集。

第二周验收标准：

- [ ] 每个回答都能看到对应的检索片段。
- [ ] 用户能判断回答是否真的来自知识库。
- [ ] 可以调节 `k` 并观察回答变化。
- [ ] 已有一份最小评估问题集，例如 10 到 20 个问题。

### 第三周：知识库管理和检索优化

目标：从“命令行建库”升级到“页面内管理知识库”，并开始系统性优化检索效果。

- [ ] 增加页面上传 PDF 和 Markdown 文件的能力。
- [ ] 支持页面触发文档切分、embedding 和向量库重建。
- [ ] 展示已入库文件列表。
- [ ] 展示知识库统计信息，例如文档数、chunk 数、更新时间。
- [ ] 支持清空并重建向量库。
- [ ] 把 chunk size 和 chunk overlap 做成可配置参数。
- [ ] 对比不同 chunk size / overlap 对检索结果的影响。
- [ ] 对比不同 `k` 值对回答质量和 token 成本的影响。
- [ ] 尝试相似度阈值过滤，避免低相关片段进入 prompt。
- [ ] 记录每轮检索的耗时、返回片段数量和最终回答耗时。
- [ ] 整理检索失败案例，标记是切分问题、embedding 问题、问题改写问题还是 prompt 问题。

第三周验收标准：

- [ ] 可以在页面内上传文档并重建知识库。
- [ ] 可以看到知识库统计信息。
- [ ] 至少完成 2 到 3 组检索参数对比实验。
- [ ] 能说清楚当前默认 `k`、chunk size、chunk overlap 的选择依据。

### 第四周：RAG 评估、优化和产品化雏形

目标：建立一套能持续使用的 RAG 评估方法，并根据评估结果优化系统。

- [ ] 扩展评估问题集，覆盖事实查询、概念解释、跨段落综合、无答案问题。
- [ ] 给每个评估问题标注期望命中的文档或片段。
- [ ] 设计检索评估指标：命中率、Top-K 命中率、无关片段比例。
- [ ] 设计回答评估指标：忠实度、完整性、简洁性、是否引用来源。
- [ ] 设计系统指标：响应耗时、token 成本、失败率。
- [ ] 增加评估脚本或评估页面，批量运行固定问题集。
- [ ] 输出不同参数组合的评估结果表格。
- [ ] 根据评估结果调整 chunk 参数、`k` 值、prompt 和回答模式。
- [ ] 增加回答引用编号，例如 `[1]`、`[2]` 对应检索片段。
- [ ] 支持导出问答记录和评估结果。
- [ ] 规划多会话保存、历史恢复、多模型切换等产品化功能。

第四周验收标准：

- [ ] 有一份可重复运行的 RAG 评估集。
- [ ] 能用表格比较不同检索参数的效果。
- [ ] 回答能带来源引用。
- [ ] 能根据评估结果说明下一轮优化方向。

## 月度交付物

- [ ] 一个更稳定的 Streamlit RAG 聊天页面。
- [ ] 一个可解释的检索依据展示模块。
- [ ] 一个页面内知识库管理入口。
- [ ] 一份 RAG 评估问题集。
- [ ] 一份检索参数对比记录。
- [ ] 一套初步评估指标：检索命中、回答忠实度、响应耗时和成本。
- [ ] 一份下一轮优化清单。

## RAG 评估和优化重点

后续评估不要只看“模型回答得像不像”，要拆成三个层面看。

检索层：

- [ ] 评估用户问题是否能命中正确文档。
- [ ] 评估 Top-K 片段里有多少真正相关。
- [ ] 评估是否有低相关片段污染 prompt。

生成层：

- [ ] 评估回答是否严格基于检索片段。
- [ ] 评估回答是否遗漏关键信息。
- [ ] 评估回答是否把模型常识和知识库内容混在一起。

系统层：

- [ ] 评估响应速度是否可接受。
- [ ] 评估 token 成本是否过高。
- [ ] 评估失败时是否能给出清晰提示。

推荐优化顺序：

1. 先优化检索命中率。
2. 再优化上下文片段质量。
3. 再优化 prompt 和回答格式。
4. 最后再做多模型、多会话、导出等产品化功能。
